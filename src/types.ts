export interface SelectOption {
	value: string;
	label: string;
}

export interface BeaconCurrency {
	name: string;
	short_code: string;
	symbol: string;
	symbol_first: boolean;
	precision: number;
}
